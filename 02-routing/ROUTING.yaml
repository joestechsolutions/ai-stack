# ROUTING.yaml — the single source of truth for "which model handles what"
#
# Why this file exists:
#   When deepseek-v4-flash gets replaced by deepseek-v5, you change ONE line here
#   (or in the fallback chain) and every cron / skill / delegate_task that points
#   to "cron:default" picks it up. Agents and frameworks are consumable;
#   this routing table is the architecture.
#
# Schema:
#   task_kind:    logical task type (cron:default, cron:weekly, agent:heavy, agent:cheap, ...)
#   model:        primary model identifier
#   provider:     provider name (must exist in ~/.hermes/config.yaml providers: section)
#   skills:       skills auto-loaded for this task kind
#   toolsets:     tool surface exposed to the agent
#   fallback:     ordered list of (provider, model) pairs tried on primary failure
#   notes:        human-readable reasoning — what this tier is good/bad at
#
# Provider notes (2026-06-16):
#   nvidia/deepseek-v4-flash  — fast, free, good for short structured tasks. Hits 429 under
#                                concurrent load (>2 simultaneous calls).
#   ollama-launch/*           — local ollama + Ollama Cloud. Cloud models are the default
#                                for chat/agent work; local models only for offline/cheap
#                                structured output (bakeoff, vision, etc.).
#   custom:minimax-m3:cloud   — primary chat model. Default for interactive Telegram
#                                sessions. (Note: agentic-heavy work goes to fcc.)

task_routing:

  # ─── Cron jobs (no human in loop, time-bounded) ────────────────────────
  cron:default: &cron_default
    model: glm-5.2:cloud
    provider: ollama-launch
    skills: []            # cron jobs attach their own skill via the job config
    toolsets: [terminal, file]
    fallback:
      - provider: ollama-launch
        model: kimi-k2.6:cloud
      - provider: ollama-launch
        model: qwen3.5:cloud
      - provider: nvidia
        model: deepseek-ai/deepseek-v4-flash
    notes: |
      Ollama Cloud is 4-6x more credit-efficient than proprietary APIs.
      Use glm-5.2:cloud as the primary workhorse for cron. Fallback chain
      keeps Ollama cloud options first, then NVIDIA for rate-limit relief.

  cron:heavy:
    model: glm-5.2:cloud
    provider: ollama-launch
    skills: [joe-content-lane]
    toolsets: [terminal, file]
    fallback:
      - provider: ollama-launch
        model: kimi-k2.6:cloud
      - provider: nvidia
        model: deepseek-ai/deepseek-v4-flash
    notes: |
      Weekly jobs that need 3-question dialogue (Quadient check-in).
      glm-5.2:cloud first; falls back through Ollama cloud and NVIDIA.

  # ─── Interactive agent (Telegram / API sessions) ──────────────────────
  agent:default:
    model: glm-5.2:cloud
    provider: ollama-launch
    skills: [joe-cto-posture]    # voice + tier-routing + posture
    toolsets: [terminal, file, web, kanban, browser, cronjob]
    fallback:
      - provider: ollama-launch
        model: kimi-k2.7-code:cloud
      - provider: ollama-launch
        model: qwen3.5:cloud
      - provider: nvidia
        model: deepseek-ai/deepseek-v4-flash
    notes: |
      Default Telegram/API sessions route through Ollama Cloud glm-5.2
      for maximum credit efficiency. Escalates to fcc for multi-step coding
      work (handled at the agent level).

  # ─── Subagent / fcc delegation ───────────────────────────────────────
  agent:coding:
    model: fcc/claude-sonnet-4
    provider: anthropic
    skills: [superpowers:writing-plans, superpowers:test-driven-development]
    toolsets: [terminal, file, web, browser, kanban, gitnexus]
    fallback: []
    notes: |
      Multi-step coding work goes through the fcc harness (port :8082).
      The fcc proxy handles model selection internally (Opus / Sonnet / Haiku
      per task). Use delegate_task(goal=..., toolsets=[...]) for all coding
      work — never code directly in the main agent.

  # ─── Cheap structured output (bakeoffs, parsers, validators) ─────────
  agent:cheap:
    model: glm-5.2:cloud
    provider: ollama-launch
    skills: []
    toolsets: [terminal, file]
    fallback:
      - provider: ollama-launch
        model: qwen3.5:cloud
      - provider: ollama-launch
        model: gpt-oss:20b-cloud
    notes: |
      Cheap structured-output tasks now default to glm-5.2:cloud because
      Ollama Cloud is the most credit-efficient option. qwen3.5 and gpt-oss
      remain as ultra-cheap fallbacks.

# ─── Live binding (read-only, generated) ──────────────────────────────
# These are mirrored from ~/.hermes/config.yaml and the cron job configs.
# Source of truth for these still lives there; this is documentation.
live_bindings:
  config_yaml: 02-routing/hermes-config.yaml          # symlink
  cron_jobs:   02-routing/cron-jobs.json              # symlink
  fallback_chain: 02-routing/hermes-config.yaml#fallback_providers

# ─── Cron → tier (derived from ~/.hermes/cron/jobs.json) ─────────────
# Regenerated 2026-07-28. Each job pins its own model: field — this table is
# the readable mirror, not the source of truth. Read jobs.json before trusting
# it. Tier rule: judgement work runs on the reasoning model, execution work on
# the code model, reflex checks on the cheap model.
cron_to_routing:

  # judgement: glm-5.2:cloud — reasoning, strategy, review
  cmo_weekly_review:              judgement
  cos_daily_standup:              judgement
  evening_plan:                   judgement
  jts_cfo_monthly_review:         judgement
  jts_coo_weekly_review:          judgement
  system-security-sweep:          judgement
  tech_intel_brief:               judgement
  vp_engineering_sprint_review:   judgement
  vp_product_monthly_review:      judgement
  weekly-job-search-sweep:        judgement

  # execution: kimi-k2.7-code:cloud — drafting, code, outbound
  "Quadient Weekly Check-in":     execution
  cfo_monthly_expense_scan:       execution
  content_draft:                  execution
  jts_cold_email_sender:          execution
  jts_weekly_success_report:      execution
  renfaire_content_draft:         execution

  # reflex: gemma4:cloud — cheap recurring checks
  ig_auto_reply:                  reflex
  jts_cold_email_reply_check:     reflex
  spend_caps_enforcement:         reflex
  vp_infrastructure_daily_health: reflex

  # bench: deepseek-v4-flash:cloud — benchmark harness only
  local_model_bakeoff_weekly:     bench

  # inherit: no pin — inherits model.default (glm-5.2:cloud)
  # (cron-failure-watch is no_agent: a script, no model, no tokens. It alerts
  #  on any job whose run was not ok and re-arms transient failures once.)
  agent_scorecard:                inherit
  cron-failure-watch:             inherit
  daily-update-check:             inherit
  fcc-upstream-tracker:           inherit
  gritt-email-watch:              inherit
  kimi-k3-tier-watch:             inherit
  skillopt-sleep-nightly:         inherit
  skillspector_weekly_scan:       inherit
  weekly-config-sync:             inherit
  weekly_balance_check:           inherit

# ─── Migration cheatsheet ────────────────────────────────────────────
# When a model dies, the change is:
#   1. Update providers: section in ~/.hermes/config.yaml
#   2. Update this file (or just the cron_jobs JSON, if it's a cron change)
#   3. Restart: systemctl --user restart hermes-gateway
#   4. Verify: bash ~/hermes-health.sh
